// Test stub for the `server-only` package. In a Next.js build this module is
// a compile-time guard with no runtime exports; Vitest can't resolve it, so
// the vitest config aliases `server-only` to this empty module.
export {};
